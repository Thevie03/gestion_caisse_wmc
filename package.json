{
    "private": true,
    "scripts": {
        "dev": "echo 'Bootstrap CDN is used - no build process needed'",
        "build": "echo 'Bootstrap CDN is used - no build process needed'"
    },
    "dependencies": {
        "axios": "^1.7.0"
    }
}
